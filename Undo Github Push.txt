You’ve moved local `main` to `f70ab09`. To make GitHub match that and drop the 4 later commits:

**1. Ensure local `main` is at the right commit**

```bash
git checkout main
git reset --hard f70ab0940e56e186a5b4bb23d0d6612c04168be7
```

**2. Overwrite remote `main` with that history**

```bash
git push --force-with-lease origin main
```

`--force-with-lease` overwrites the remote branch but refuses to push if the remote has new commits you don’t have, which avoids overwriting someone else’s work.

**If you get “rejected”:**  
Someone (or another clone) pushed after your last fetch. Then either:

- `git fetch origin` and check `git log main..origin/main`; if you’re sure you want to drop those remote commits, use  
  `git push --force origin main`  
  (only if you’re certain no one else depends on those 4 commits).

**Before you do this:**

- Those 4 commits will disappear from `main` on GitHub.
- Anyone who has pulled them will have a diverged history and will need to reset their local `main` to match (e.g. `git fetch origin && git reset --hard origin/main`).
- If others use this repo, coordinate with them first.